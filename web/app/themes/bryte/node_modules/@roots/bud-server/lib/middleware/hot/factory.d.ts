import type { MultiCompiler, StatsCompilation, StatsModule } from '@roots/bud-framework/config';
import type { MiddlewareFactory } from '@roots/bud-server/middleware';
import type { Payload } from '@roots/bud-server/middleware/hot';
import type { Handler } from 'express-serve-static-core';
import { HotEventStream } from '@roots/bud-server/middleware/hot';
export declare const factory: MiddlewareFactory;
export declare const makeHandler: (compiler: MultiCompiler) => Handler;
export declare const publish: (action: Payload['action'], statsCompilation: StatsCompilation, stream: HotEventStream) => void;
export declare const collectModules: (modules?: Array<StatsModule>) => StatsModule;
export declare const collectCompilations: (stats: StatsCompilation) => Array<StatsCompilation>;
