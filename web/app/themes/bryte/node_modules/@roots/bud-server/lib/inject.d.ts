import type { Bud } from '@roots/bud-framework';
/**
 * Inject webpack entrypoints with client scripts
 */
export declare const inject: (app: Bud, injection: ((app: Bud) => false | string)[]) => Promise<void>;
