/**
 * `@roots/bud-wordpress-dependencies-webpack-plugin`
 *
 * @see https://bud.js.org
 */
import { Extension, type Option } from '@roots/bud-framework/extension';
import { type Options, default as WordPressDependenciesWebpackPlugin } from '@roots/wordpress-dependencies-webpack-plugin';
export default class BudWordPressDependencies extends Extension<Options, WordPressDependenciesWebpackPlugin> {
    exclude: Option<BudWordPressDependencies, Options, `exclude`>[`value`];
    getExclude: Option<BudWordPressDependencies, Options, `exclude`>[`get`];
    setExclude: Option<BudWordPressDependencies, Options, `exclude`>[`set`];
    emitWordPressJson: Option<BudWordPressDependencies, Options, `emitWordPressJson`>[`value`];
    getEmitWordPressJson: Option<BudWordPressDependencies, Options, `emitWordPressJson`>[`get`];
    setEmitWordPressJson: Option<BudWordPressDependencies, Options, `emitWordPressJson`>[`set`];
    entrypointsPlugin: Option<BudWordPressDependencies, Options, `entrypointsPlugin`>[`value`];
    getEntrypointsPlugin: Option<BudWordPressDependencies, Options, `entrypointsPlugin`>[`set`];
    setEntrypointsPlugin: Option<BudWordPressDependencies, Options, `entrypointsPlugin`>[`get`];
}
