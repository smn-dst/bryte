import { Bud } from '@roots/bud-framework';
export interface bindFacade {
    (this: Bud, key: `${keyof Bud & string}`, fn: CallableFunction, binding?: unknown): Bud;
}
/**
 * Bind a synchronous facade
 */
export declare const bindFacade: bindFacade;
