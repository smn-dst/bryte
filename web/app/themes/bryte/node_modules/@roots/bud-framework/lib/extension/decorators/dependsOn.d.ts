import type { Modules } from '@roots/bud-framework/registry/modules';
export declare const dependsOn: (dependsOn: Array<`${keyof Modules & string}`>) => <Type extends new (...args: any[]) => any>(constructor: Type) => {
    new (...args: any[]): {
        [x: string]: any;
        dependsOn: Set<string>;
    };
} & Type;
