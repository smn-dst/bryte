/**
 * A decorator that adds a `label` property to the class.
 *
 * @param label - Extension label
 */
export const label = (label) => (constructor) => class extends constructor {
    label = label;
};
