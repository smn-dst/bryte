export const useLongestNamedObjectLength = (items = []) => longestNamedObjectLength(items);
export const longestNamedObjectLength = (items = []) => items?.reduce((longest, item) => {
    const length = item?.name?.length;
    if (!length)
        return longest;
    return Math.max(length, longest);
}, 0) + 1;
