import type { StatsAsset, StatsCompilation } from '@roots/bud-framework/config';
export interface Props {
    basedir?: string;
    borderColor?: string;
    compact?: boolean;
    compilation: StatsCompilation;
    debug?: boolean;
    displayAssets?: boolean;
    displayEntrypoints?: boolean;
    id: number;
    total?: number;
}
export interface Asset extends Partial<StatsAsset> {
}
export interface AssetGroup {
    assets?: Array<Asset>;
}
declare const Compilation: ({ basedir, compact, compilation, displayAssets, displayEntrypoints, id, total, }: Props) => import("@roots/bud-support/jsx-runtime").JSX.Element;
export default Compilation;
