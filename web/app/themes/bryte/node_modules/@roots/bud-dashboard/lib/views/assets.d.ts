import type { StatsCompilation } from '@roots/bud-framework/config';
export interface Props {
    compact?: boolean;
    compilation?: StatsCompilation;
    displayAssets?: boolean;
    limit?: number;
}
export default function CompilationAssets({ compact, compilation, displayAssets, limit, }: Props): import("@roots/bud-support/jsx-runtime").JSX.Element | null;
