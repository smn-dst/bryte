export function define(values) {
    const define = this.extensions.get(`@roots/bud-extensions/webpack-define-plugin`);
    Object.entries(values).forEach(([key, value]) => {
        define.set(key, value);
    });
    return this;
}
