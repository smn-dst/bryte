import type { Bud } from '@roots/bud-framework';
import type { Compiler } from '@roots/bud-support/webpack';
import { Extension } from '@roots/bud-framework/extension';
import { type SettingsAndStyles } from '@roots/wordpress-theme-json-webpack-plugin';
/**
 * Support Tailwind values in {@link Bud.wpjson}
 */
export declare class TailwindThemeJSON extends Extension {
    /**
     * {@link Extension.apply}
     */
    apply(compiler: Compiler): void;
    /**
     * {@link Extension.register}
     */
    register(bud: Bud): Promise<void>;
    tapTailwindColor(options: SettingsAndStyles): SettingsAndStyles;
    tapTailwindFontFamily(json: SettingsAndStyles): SettingsAndStyles;
    tapTailwindFontSize(options: SettingsAndStyles): SettingsAndStyles;
    tapTailwindSpacing(options: SettingsAndStyles): SettingsAndStyles;
    useTailwindColors(extend?: boolean): any;
    useTailwindFontFamily(extend?: boolean): any;
    useTailwindFontSize(extend?: `extend` | boolean): any;
    useTailwindSpacing(extend?: boolean): any;
    private handleOption;
}
