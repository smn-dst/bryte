export const transformEntry = ([slug, fontSize]) => ({
    name: slug,
    size: fontSize,
    slug,
});
export const transform = fonts => Object.entries(fonts)
    .map(([k, v]) => [k, Array.isArray(v) ? v[0] : v])
    .map(transformEntry);
