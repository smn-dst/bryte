import type { Bud } from '@roots/bud';
export type Override<T extends unknown> = [
    T,
    string,
    string,
    (bud: Bud) => (value: T) => Promise<unknown>
];
export default function override(bud: Bud, arg: unknown, env: string, manifestPath: string | undefined, callback: (bud: Bud) => (value: any) => Promise<any>): Promise<void>;
/**
 * Override settings:
 *
 * - when children: all children but not the parent
 * - when no children: the parent;
 */
export declare const withChildren: (bud: Bud, value: any, makeFn: (bud: Bud) => (value: any) => Promise<any>) => Promise<void>;
