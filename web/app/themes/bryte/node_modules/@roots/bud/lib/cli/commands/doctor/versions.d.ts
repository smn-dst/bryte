import type { Bud } from '@roots/bud-framework';
interface PackageResult {
    error: boolean;
    packageVersion: string;
    requiredVersion: string;
    signifier: string;
}
export declare const Versions: ({ packages, }: {
    packages?: Array<PackageResult>;
}) => import("@roots/bud-support/jsx-runtime").JSX.Element;
export declare const criticalPackages: string[];
export declare const getPackageResults: (this: Bud, signifier: string) => Promise<PackageResult>;
export {};
