import { jsx as _jsx, jsxs as _jsxs } from "@roots/bud-support/jsx-runtime";
/* eslint-disable react/no-unescaped-entities */
import { Display as Error } from '@roots/bud-dashboard/components/error';
import { Text } from '@roots/bud-support/ink';
import { LabelBox } from '../../components/LabelBox.js';
export const BuildInfo = ({ error, name, time, }) => {
    if (error) {
        return (_jsx(LabelBox, { color: "red", children: _jsx(Error, { error: error }) }));
    }
    return (_jsx(LabelBox, { label: name, children: _jsxs(Text, { italic: true, children: ["Completed a dry run of your project's build (executed in ", time, ` `, "seconds)."] }) }));
};
