import type { Bud, WebpackPluginInstance } from '@roots/bud-framework';
import { Extension } from '@roots/bud-framework/extension';
/**
 * Webpack profile plugin configuration
 */
export default class BudProfile extends Extension<Record<string, any>, WebpackPluginInstance> {
    /**
     * {@link Extension.make}
     */
    make(bud: Bud, options: Record<string, any>): Promise<any>;
    /**
     * {@link Extension.when}
     */
    when(bud: Bud): boolean;
}
