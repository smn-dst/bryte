import { type build } from '@roots/bud-support/esbuild-wasm';
interface transformer {
    build: typeof build;
}
declare let transformer: transformer;
export declare const getImplementation: (context: string) => Promise<transformer>;
export { transformer };
