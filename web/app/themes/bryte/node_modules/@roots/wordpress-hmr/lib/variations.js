import * as editor from '@roots/wordpress-hmr/editor';
import * as api from '@roots/wordpress-hmr/variation';
export const register = (getContext, accept) => editor.load({ accept, api, getContext });
