import { Extension, type Option } from '@roots/bud-framework/extension';
import WordPressExternals, { type Options } from '@roots/wordpress-externals-webpack-plugin';
export default class BudWordPressExternals extends Extension<Options, WordPressExternals> {
    /**
     * Excluded packages
     */
    exclude: Option<BudWordPressExternals, Options, `exclude`>[`value`];
    /**
     * Get excluded packages
     *
     * @example
     * ```js
     * extension.getExclude()
     * ```
     */
    getExclude: Option<BudWordPressExternals, Options, `exclude`>[`get`];
    /**
     * Set excluded packages
     *
     * @param exclude - Array or callback function that returns an array
     *
     * @example
     * ```js
     * extension.setExclude(['jquery'])
     * ```
     *
     * @example
     * ```js
     * extension.setExclude((exclude = []) => [...exclude, 'jquery'])
     * ```
     */
    setExclude: Option<BudWordPressExternals, Options, `exclude`>[`set`];
}
